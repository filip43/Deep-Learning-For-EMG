Myo Data Capture
----------------

This is a simple data collection application that will log the EMG and IMU data from a Myo armband to timestamped files in your home directory.

It will only work with one armband. If it re-connects to an armband, it will create a new set of log files.